pre-commit:
  parallel: true
  commands:
    check-format:
      glob: "**/*.py"
      run: poetry run ruff format {staged_files}
    lint:
      glob: "**/*.py"
      run: poetry run ruff check --fix {staged_files}
